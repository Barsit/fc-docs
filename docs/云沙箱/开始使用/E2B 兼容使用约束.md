# E2B 兼容使用约束

## **支持地域**

### **通过 E2B SDK 使用云沙箱（New）**

当前云沙箱 API/SDK 仅在以下地域可用：

- 华北2（北京）

## **兼容 E2B SDK的版本约束**

使用云沙箱功能需要满足以下 SDK 版本要求：

- Python SDK（e2b）：≥ 2.20.1
- TypeScript/Node.js SDK（e2b）：≥ 2.20.1
